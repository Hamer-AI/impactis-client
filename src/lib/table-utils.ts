/**
 * Utilities for TanStack Table integration.
 * Use with @tanstack/react-table when building list/table UIs (e.g. core team, invites, discovery list).
 *
 * @example
 * import { useReactTable, getCoreRowModel, flexRender } from '@tanstack/react-table'
 * import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
 *
 * const table = useReactTable({
 *   data,
 *   columns,
 *   getCoreRowModel: getCoreRowModel(),
 * })
 * // Render with Table, TableHeader, TableBody, flexRender(table.getHeaderGroups(), ...)
 */
export {}
